---
import Shell from '../components/Shell.astro';
import AuditForm from '../components/AuditForm';

const trustCards = [
  {
    title: 'Open source (MIT)',
    desc: 'Full methodology, scoring weights, and source code on GitHub. No proprietary scoring.',
    link: { label: 'View on GitHub', href: 'https://github.com/Auriti-Labs/geo-optimizer-skill' },
  },
  {
    title: 'Research-backed',
    desc: 'Scoring grounded in Princeton KDD 2024 and AutoGEO ICLR 2026 research on AI search retrieval.',
    link: { label: 'Read the research', href: '/research' },
  },
  {
    title: 'Auditable scoring',
    desc: 'Every signal is traceable to a published rule. Score breakdowns show per-category contributions.',
    link: null,
  },
  {
    title: 'Machine-readable output',
    desc: 'JSON format designed for CI/CD pipelines, integrations, and the GeoReady platform API.',
    link: null,
  },
];

const actions = [
  {
    label: 'Run a free audit',
    desc: 'Enter any public URL above — no account required.',
    href: '#audit-form',
    badge: 'Available now',
    badgeStyle: 'bg-accent-success/10 text-accent-success',
  },
  {
    label: 'Use the open-source CLI',
    desc: 'pip install geo-optimizer-skill — MIT licensed, unlimited audits.',
    href: 'https://github.com/Auriti-Labs/geo-optimizer-skill',
    badge: 'Free forever',
    badgeStyle: 'bg-accent-success/10 text-accent-success',
  },
  {
    label: 'Read the research',
    desc: 'Princeton KDD 2024 and AutoGEO ICLR 2026 behind the scoring methodology.',
    href: '/research',
    badge: 'Documentation',
    badgeStyle: 'bg-bg-subtle text-text-muted border border-border',
  },
  {
    label: 'Join early access',
    desc: 'Pro monitoring, history, and alerts — planned, not yet available.',
    href: '/early-access',
    badge: 'Early access',
    badgeStyle: 'bg-accent-teal/10 text-accent-teal',
  },
];

const faqs = [
  {
    q: 'What is Generative Engine Optimization?',
    a: 'GEO is the practice of making websites discoverable, readable, and citable by AI search engines such as ChatGPT, Perplexity, Claude, and Gemini. It focuses on structured data, content hierarchy, and technical signals that AI crawlers prioritize.',
  },
  {
    q: 'What does GEO Optimizer check?',
    a: 'The tool audits 8 categories: robots.txt, llms.txt, JSON-LD schema, meta tags, content quality, technical signals, AI discovery paths, and brand entity signals. It also detects prompt injection patterns and negative signals.',
  },
  {
    q: 'How is GEO different from traditional SEO?',
    a: 'Traditional SEO optimizes for keyword rankings and backlinks in conventional search engines. GEO optimizes for AI citation signals: structured data, clear content architecture, AI crawler permissions, and self-contained answer blocks. The two complement rather than replace each other.',
  },
  {
    q: 'Is the Pro plan available now?',
    a: 'No. GeoReady Pro — which adds weekly monitoring, score history, and regression alerts — is in early access planning. You can join the waitlist at geoready.dev/early-access to be notified when it launches. No payment is collected until the plan goes live.',
  },
  {
    q: 'Is GEO Optimizer open source?',
    a: 'Yes. The core toolkit is MIT licensed. Source code, scoring weights, and methodology are publicly available on GitHub. The CLI and web audit remain free.',
  },
];
---

<Shell
  title="GEO Optimizer — AI Search Visibility & Citability Audit"
  description="Audit your website's AI search visibility and citability. GEO Optimizer scores your site across 8 categories with actionable recommendations — free, no account required."
  canonical="https://geoready.dev"
>
  {/* Hero */}
  <section id="audit-form" class="bg-bg-dark text-text-inverse">
    <div class="max-w-7xl mx-auto px-6 py-20 md:py-28">
      <div class="max-w-3xl">
        <h1 class="text-4xl md:text-6xl font-bold tracking-tight leading-tight">
          Is your website visible to AI search engines?
        </h1>
        <p class="mt-6 text-lg md:text-xl text-text-muted leading-relaxed max-w-2xl">
          GEO Optimizer audits whether a website can be crawled, understood, and cited by AI answer engines.
          Get a 0–100 score across 8 signal categories — free, no account required.
        </p>

        <div class="mt-10">
          <AuditForm client:load />
        </div>

        <div class="mt-8 flex flex-wrap items-center gap-6">
          <div class="flex items-center gap-2 text-sm">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" class="text-yellow-400 shrink-0" aria-hidden="true">
              <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
            </svg>
            <span class="font-mono font-semibold">407</span>
            <span class="text-text-muted">stars</span>
          </div>
          <div class="flex items-center gap-2 text-sm">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-blue-400 shrink-0" aria-hidden="true">
              <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/>
              <polyline points="3.27 6.96 12 12.01 20.73 6.96"/>
              <line x1="12" y1="22.08" x2="12" y2="12"/>
            </svg>
            <span class="font-mono font-semibold">4.0k</span>
            <span class="text-text-muted">downloads/mo</span>
          </div>
          <div class="flex items-center gap-2 text-sm">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-green-400 shrink-0" aria-hidden="true">
              <circle cx="11" cy="11" r="8"/>
              <line x1="21" y1="21" x2="16.65" y2="16.65"/>
            </svg>
            <span class="font-mono font-semibold">1.6k</span>
            <span class="text-text-muted">audits</span>
          </div>
        </div>
        <p class="mt-2 text-xs text-text-muted font-mono">Snapshot: May 2026 — not live data</p>

        <div class="mt-6 flex flex-wrap items-center gap-4 text-sm">
          <a href="/pricing" class="text-text-muted hover:text-text-inverse transition-colors">View pricing</a>
          <span class="text-text-muted/40">·</span>
          <a href="/early-access" class="text-text-muted hover:text-text-inverse transition-colors">Join early access</a>
        </div>
      </div>
    </div>
  </section>

  {/* What is AI search visibility */}
  <section class="max-w-7xl mx-auto px-6 py-16">
    <div class="max-w-3xl">
      <h2 class="text-2xl md:text-3xl font-bold text-text-primary">What is AI search visibility?</h2>
      <p class="mt-4 text-text-secondary leading-relaxed">
        Traditional SEO optimizes for ranking in conventional search result pages — crawlability, backlinks, keyword signals.
        AI search visibility is a different question: whether an AI answer engine can read, parse, understand, and <em>cite</em> your content when generating a response.
      </p>
      <p class="mt-4 text-text-secondary leading-relaxed">
        A site can rank well on Google and still be largely opaque to AI systems.
        The relevant signals are different: a well-configured <code class="font-mono text-sm bg-bg-subtle px-1 rounded">robots.txt</code> that permits AI crawlers,
        an <code class="font-mono text-sm bg-bg-subtle px-1 rounded">llms.txt</code> file that guides them through your content,
        complete JSON-LD schema for entity disambiguation, factual density, and brand coherence.
        GEO Optimizer audits every one of these signals and produces a 0–100 score with per-category breakdown.
      </p>
      <div class="mt-6 flex flex-wrap items-center gap-4 text-sm">
        <a href="/research" class="text-accent-teal hover:text-accent-teal-dark transition-colors">Read the research</a>
        <span class="text-text-muted">·</span>
        <a href="/manifesto" class="text-accent-teal hover:text-accent-teal-dark transition-colors">Read the manifesto</a>
        <span class="text-text-muted">·</span>
        <a href="/report/demo" class="text-accent-teal hover:text-accent-teal-dark transition-colors">View a demo report</a>
      </div>
    </div>
  </section>

  {/* How it works */}
  <section class="max-w-7xl mx-auto px-6 py-16">
    <div class="max-w-3xl">
      <h2 class="text-2xl md:text-3xl font-bold text-text-primary">How GEO Optimizer works</h2>
      <div class="mt-8 space-y-8">
        <div>
          <h3 class="text-lg font-semibold text-text-primary">1. Enter a URL</h3>
          <p class="mt-2 text-text-secondary leading-relaxed">
            Submit any public website address. The tool resolves and validates the URL,
            then fetches the page source for analysis.
          </p>
        </div>
        <div>
          <h3 class="text-lg font-semibold text-text-primary">2. Get a GEO score</h3>
          <p class="mt-2 text-text-secondary leading-relaxed">
            The audit evaluates 8 signal categories — robots.txt, llms.txt, schema, meta tags,
            content quality, technical signals, AI discovery, and brand entity — producing a
            0–100 score with per-category breakdown.
          </p>
        </div>
        <div>
          <h3 class="text-lg font-semibold text-text-primary">3. Apply prioritized fixes</h3>
          <p class="mt-2 text-text-secondary leading-relaxed">
            Each finding is ranked by impact and assigned a severity level. The report includes
            actionable recommendations with specific file changes and schema additions.
          </p>
        </div>
      </div>
    </div>
  </section>

  {/* Trust cards */}
  <section class="max-w-7xl mx-auto px-6 py-20">
    <h2 class="text-xl md:text-2xl font-bold text-text-primary mb-8">Why trust this tool</h2>
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
      {trustCards.map((c) => (
        <div class="p-6 rounded-[--radius-lg] border border-border bg-bg-surface hover:border-accent-teal/40 transition-colors flex flex-col">
          <h3 class="font-display text-base font-semibold text-text-primary">{c.title}</h3>
          <p class="mt-2 text-sm text-text-secondary leading-relaxed flex-1">{c.desc}</p>
          {c.link && (
            <a
              href={c.link.href}
              target={c.link.href.startsWith('http') ? '_blank' : undefined}
              rel={c.link.href.startsWith('http') ? 'noopener' : undefined}
              class="mt-4 text-xs font-mono text-accent-teal hover:underline"
            >
              {c.link.label} →
            </a>
          )}
        </div>
      ))}
    </div>
  </section>

  {/* What you can do today */}
  <section class="max-w-7xl mx-auto px-6 py-16 border-t border-border">
    <h2 class="text-xl md:text-2xl font-bold text-text-primary mb-8">What you can do today</h2>
    <div class="grid sm:grid-cols-2 gap-4">
      {actions.map((a) => (
        <a
          href={a.href}
          target={a.href.startsWith('http') ? '_blank' : undefined}
          rel={a.href.startsWith('http') ? 'noopener' : undefined}
          class="group flex items-start gap-4 p-5 rounded-[--radius-lg] border border-border bg-bg-surface hover:border-accent-teal/40 transition-colors"
        >
          <div class="flex-1 min-w-0">
            <div class="flex items-center gap-2 mb-1 flex-wrap">
              <p class="text-sm font-semibold text-text-primary group-hover:text-accent-teal transition-colors">{a.label}</p>
              <span class={`text-[10px] font-mono uppercase tracking-wider px-1.5 py-0.5 rounded-full ${a.badgeStyle}`}>{a.badge}</span>
            </div>
            <p class="text-sm text-text-secondary leading-relaxed">{a.desc}</p>
          </div>
          <svg class="mt-0.5 w-4 h-4 shrink-0 text-text-muted group-hover:text-accent-teal transition-colors" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>
          </svg>
        </a>
      ))}
    </div>
  </section>

  {/* From audit to monitoring */}
  <section class="max-w-7xl mx-auto px-6 py-16 border-t border-border">
    <h2 class="text-xl md:text-2xl font-bold text-text-primary mb-2">From audit to monitoring</h2>
    <p class="text-sm text-text-secondary mb-8 max-w-2xl">
      The free audit is a point-in-time diagnosis. Monitoring is what turns it into an ongoing signal.
    </p>
    <div class="grid md:grid-cols-2 gap-6">
      <div class="p-6 rounded-[--radius-lg] border border-border bg-bg-surface">
        <span class="text-xs font-mono font-semibold uppercase tracking-wider px-2 py-0.5 rounded-full bg-accent-success/10 text-accent-success inline-block mb-4">
          Available now
        </span>
        <h3 class="text-base font-semibold text-text-primary mb-3">Free audit</h3>
        <ul class="space-y-2 text-sm text-text-secondary">
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-accent-teal shrink-0">✓</span>
            GEO score 0–100 for any public URL
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-accent-teal shrink-0">✓</span>
            Free report preview with top recommendations
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-accent-teal shrink-0">✓</span>
            No account, no credit card
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-accent-teal shrink-0">✓</span>
            CLI (MIT, unlimited) via pip
          </li>
        </ul>
      </div>
      <div class="p-6 rounded-[--radius-lg] border border-border bg-bg-surface">
        <span class="text-xs font-mono font-semibold uppercase tracking-wider px-2 py-0.5 rounded-full bg-accent-teal/10 text-accent-teal inline-block mb-4">
          Early access — planned
        </span>
        <h3 class="text-base font-semibold text-text-primary mb-3">Pro monitoring</h3>
        <ul class="space-y-2 text-sm text-text-secondary">
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-accent-teal shrink-0">+</span>
            Weekly monitoring — score tracked automatically
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-accent-teal shrink-0">+</span>
            Full 8-category report history
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-accent-teal shrink-0">+</span>
            Regression alerts when score drops
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-accent-teal shrink-0">+</span>
            PDF export and agency reporting
          </li>
        </ul>
        <a href="/early-access" class="mt-5 inline-block text-sm text-accent-teal hover:underline font-medium">
          Join the early access list →
        </a>
      </div>
    </div>
  </section>

  {/* FAQ */}
  <section class="max-w-7xl mx-auto px-6 py-16 border-t border-border">
    <div class="max-w-3xl">
      <h2 class="text-2xl md:text-3xl font-bold text-text-primary mb-8">Frequently asked questions</h2>
      <div class="space-y-6">
        {faqs.map((faq) => (
          <div>
            <h3 class="text-base font-semibold text-text-primary">{faq.q}</h3>
            <p class="mt-2 text-sm text-text-secondary leading-relaxed">{faq.a}</p>
          </div>
        ))}
        <div>
          <h3 class="text-base font-semibold text-text-primary">Does the audit store submitted URLs?</h3>
          <p class="mt-2 text-sm text-text-secondary leading-relaxed">
            URLs are processed in real time. No persistent storage of submitted URLs is declared unless
            explicitly configured in the backend. See the <a href="/privacy" class="text-accent-teal hover:underline">Privacy Policy</a> for details.
          </p>
        </div>
      </div>
    </div>
  </section>

  {/* Final CTA */}
  <section class="max-w-7xl mx-auto px-6 pb-20">
    <div class="p-8 md:p-12 rounded-[--radius-xl] border border-border bg-bg-surface">
      <div class="max-w-3xl">
        <h2 class="text-2xl md:text-3xl font-bold text-text-primary">
          AI search is already deciding which sites get cited.
        </h2>
        <p class="mt-4 text-text-secondary leading-relaxed">
          The signals are documented, the research is published, and the audit is free.
          GEO Optimizer exists to make AI visibility auditable and accessible to every developer —
          not just teams with enterprise budgets.
        </p>
        <div class="mt-6 flex flex-wrap gap-3">
          <a
            href="#audit-form"
            class="inline-block py-2.5 px-6 rounded-[--radius-md] bg-accent-teal text-white text-sm font-semibold hover:bg-accent-teal-dark transition-colors"
          >
            Start free audit
          </a>
          <a
            href="/early-access"
            class="inline-block py-2.5 px-6 rounded-[--radius-md] border border-border text-sm font-semibold text-text-primary hover:bg-bg-subtle transition-colors"
          >
            Join early access →
          </a>
        </div>
        <p class="mt-4 text-xs text-text-muted">
          Or <a href="/manifesto" class="hover:text-text-secondary transition-colors underline">read the manifesto</a>
          <span class="mx-1">·</span>
          <a href="/pricing" class="hover:text-text-secondary transition-colors underline">view pricing</a>
        </p>
      </div>
    </div>
  </section>
</Shell>
