{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "sh -c 'alembic upgrade head && python -m uvicorn geoready_platform.api.main:app --host 0.0.0.0 --port ${PORT}'",
    "healthcheckPath": "/healthz"
  }
}
